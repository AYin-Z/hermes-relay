<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal theme — Compose handles all theming via Material 3.
         Night mode is locked from Appearance via AppCompatDelegate
         (AppearanceNightMode). forceDarkAllowed is set only in values-v29
         (API 29+); minSdk is 26 so it must not live in base values/. -->
    <style name="Theme.HermesRelay" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
